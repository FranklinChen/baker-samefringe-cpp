language: cpp
matrix:
  include:
    - os: osx
      osx_image: xcode10.2

before_install:
  - brew update
  - brew outdated boost || brew upgrade boost

script:
  - mkdir build
  - cd build
  - cmake ..
  - make
  - make test
