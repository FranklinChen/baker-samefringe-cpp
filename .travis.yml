sudo: false
language: objective-c

before_install:
  - brew update
  - brew outdated cmake || brew upgrade cmake
  - brew outdated boost || brew upgrade boost

script:
  - mkdir build
  - cd build
  - cmake ..
  - make
  - make test
